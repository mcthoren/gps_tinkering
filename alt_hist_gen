#!/usr/bin/perl -T
use strict;
use warnings;

my %el_hash;
my $elev;

while (<>) {
	chomp;
	my ($x, $y, $a, $z, $b) = split;
	$el_hash{$z}++;
}

foreach $elev (sort keys %el_hash) {
	printf "%.1f m\t%d\n", $elev, $el_hash{$elev};
}
